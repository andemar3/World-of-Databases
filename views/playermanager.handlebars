<!DOCTYPE html>
<html>
  <head>
    <title>Players</title>
  </head>
  <body>
	<p class="message-text">{{message}}</p>
    <div class="player-create">
        <form action='/playermanager' method="post">
			<h3 class="form-header">Create a Player</h3>
			<p class="form-text">Create a new player with the default starting stats</p>
			<label for="player-name">Player Name<span class="required">*</span></label>
			<input type="text" id="playerName" name="playerName" placeholder="Name" required>
			<br/>
			<input type="submit" value="Submit">
		</form>
    </div>

	<br/>
	<div class="player-delete">
		<form action="/playermanager" method="get">
			<h3 class="form-header">Delete a Player</h3>
			<p class="warning-text">Deleting a player will remove all player data including records of what items the player owned. This data cannot be retrieved once deleted. This should only be used when a player ban has been issued or a player requests a data deletion.</p>
			<label for="player-id">Player ID<span class="required">*</span></label>
			<input type="number" id="player-id" name="id" placeholder="Player ID" required>
			<br/>
			<button class="form-button" type="submit">Delete Player</button>
		</form>
	</div>
	<br/>
  </body>
</html>